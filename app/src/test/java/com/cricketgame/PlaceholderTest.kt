package com.cricketgame

import org.junit.Assert.assertTrue
import org.junit.Test

/**
 * Placeholder test — proves the CI pipeline runs tests end-to-end.
 * This test will be replaced by real domain tests once the game engine exists.
 */
class PlaceholderTest {

    @Test
    fun `project compiles and test framework works`() {
        // If this test runs, the CI pipeline is working:
        // - Gradle found the test source set
        // - JUnit is on the classpath
        // - The test runner executed successfully
        assertTrue("CI pipeline is operational", true)
    }
}
